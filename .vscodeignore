# Allowlist: ignore everything, then re-include ONLY what ships in the .vsix.
**

# Extension runtime
!package.json
!dist/extension.js
!dist/welcome.js
!dist/welcome.css
!media/**

# Marketplace metadata
!README.md
!LICENSE.md
!CHANGELOG.md

# Bundled agent plugins: ship the whole folder verbatim — these are the distributed artifacts the
# installers stage/register. Dev tooling lives in repo-root scripts/, outside this tree.
!plugins/**
